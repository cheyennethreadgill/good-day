import timeJson from "../TOD.json";
import taskData from "../task-data.json";
import TaskContainer from "./Tasks/TasksContainer";

export default function TODCOLS({ clicked, handleTimeClick, filteredTasks, id, timeOfDay }) {
  // const timeOfDayfromJSON = timeJson.map((obj) => {
  //   const { id, timeOfDay } = obj;
  //   return { id, timeOfDay };
  // });

  // filter tasks based on each timeOfDay

  //   function returnTaskContainer(ft) {
  //     if (ft === timeOfDay) {
  //       return <TaskContainer t={ft} />;
  //     }
    //   }
    
    

  return (
    <>
      
    </>
  );
}
